# CodeCoach AI — environment configuration
# No API key needed — uses Ollama locally.
#
# Make sure Ollama is running:
#   ollama serve
#   ollama pull llama3.1:8b
#
# Optional: override the default Ollama host
OLLAMA_HOST=http://localhost:11434
